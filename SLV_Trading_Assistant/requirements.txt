# SLV Trading Assistant — Python dependencies
# Rev 1

# Core data
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.26.0

# Macro data (Phase 2)
# pandas-datareader removed: distutils is absent in Python 3.13; FRED REST API used directly instead
# pandas-datareader>=0.10.0

# Configuration
python-dotenv>=1.0.0

# HTTP + news feeds (Phase 3)
requests>=2.31.0
feedparser>=6.0.10
beautifulsoup4>=4.12.0

# Technical indicators (Phase 1b)
# NOTE: verify pandas-ta compatibility with Python 3.13 before installing
pandas-ta>=0.3.14b

# Charting (Phase 4)
matplotlib>=3.7.0
plotly>=5.15.0

# Schwab brokerage API — price history + options chain (Phase 5)
schwab-py>=1.4.0
